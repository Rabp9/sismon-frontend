import { InterseccionesDisplayDirective } from './intersecciones-display.directive';

describe('InterseccionesDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new InterseccionesDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
